name: Deploy (production)

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: production

    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Set SHORT_SHA
      run: echo "SHORT_SHA=${GITHUB_SHA::7}" >> "$GITHUB_ENV"

    # 1. Set up QEMU for multi-platform emulation
    - name: Set up QEMU
      uses: docker/setup-qemu-action@v3

    # 2. Set up Docker Buildx (creates a container driver capable of multi-platform)
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: "coutito"
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build + push image to dockerhub
      uses: docker/build-push-action@v6
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: true
        no-cache: true
        tags: coutito/basement:${{ env.SHORT_SHA }}

    # Disabled for now
    # - name: Deploy on production server
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.REMOTE_HOST }}
    #     username: ${{ secrets.REMOTE_USER }}
    #     key: ${{ secrets.SSH_PRIVATE_KEY }}
    #     script: |
    #       docker pull coutito/basement:${{ env.SHORT_SHA }}
    #
    #       docker rm -f basement || true
    #
    #       docker run -d \
    #         --name basement \
    #         --platform linux/amd64 \
    #         -p 8080:3000 \
    #         -v .env:/app/.env \
    #         coutito/basement:${{ env.SHORT_SHA }}
